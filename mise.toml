[tools]
node = "24"
rust = "1.98.1"
java = "openjdk-21.0.2"
"aqua:evilmartians/lefthook" = '2'
"aqua:pnpm/pnpm" = '12.4.1'

[tasks.default]
run = "pnpm run dev"

[tasks.build]
run = "pnpm run build"

[tasks.update_dependencies]
run = ['pnpm dlx npm-check-updates -u', 'pnpm install']
